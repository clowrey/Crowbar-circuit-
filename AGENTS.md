# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Crowbar-circuit-`) is currently an empty/blank project containing only a `README.md`. There is no application code, no dependency manifests, no build system, no services, and no tests.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `pyproject.toml`, `Cargo.toml`, `go.mod`, or similar files exist yet.
- **No services to run** — no backend, frontend, database, or other services are configured.
- **No lint/test/build commands** — no tooling has been set up.

When application code is added, update this section with:
- How to install dependencies and which package manager to use.
- How to run lint, tests, and the dev server.
- Any non-obvious startup caveats or environment requirements.
